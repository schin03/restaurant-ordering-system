/** Smooth ease-out curve used across reveals. */
export const EASE_OUT = [0.22, 1, 0.36, 1]

/** Standard scroll-triggered viewport: animate once, slightly before fully in view. */
export const revealViewport = { once: true, margin: '-48px 0px', amount: 0.12 }
